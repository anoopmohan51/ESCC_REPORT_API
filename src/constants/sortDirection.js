/**
 * Sort Direction Enum Constants
 * Defines the different directions for sorting (ascending and descending)
 */

const SORT_DIRECTION = {
  Ascending: 1,
  Descending: 0
};

module.exports = SORT_DIRECTION; 