/**
 * Date Range Filter Enum Constants
 * Defines the different date range filter options
 */

const DATE_RANGE_FILTER = {
  None: 0,
  StartDate: 1,
  EndDate: 2,
  All: 3
};

module.exports = DATE_RANGE_FILTER; 